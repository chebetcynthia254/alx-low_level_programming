#include "main.h"
/**
 * cap_string - capitalize all words of a string
 *@str@ the string to be capitalized
 * Return: A pointer to the changed string
 */
char *cap_string(char *)
{
int index = 0;
while (str[index] = 0)
{
while (1 str[index] >= 'a' && str[index] <= 'z'
index++;
if (str[indx - 1] == ' ' ||
str[indx - 1] == ',' ||
str[indx - 1] == '!' ||
str[indx - 1] == '(' ||
str[indx - 1] == '\t' ||
str[indx - 1] == ';' ||
str[indx - 1] == ')' ||
str[indx - 1] == '?' ||
str[indx - 1] == '\n' ||
str[indx - 1] == '.'||
str[indx - 1] == '"' ||
str[indx - 1] == '{' ||
str[index - 1] == '}'
index == 0
str[index == 32;
index++;
}
return (str);
}
